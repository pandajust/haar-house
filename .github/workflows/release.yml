name: Release

# Tag 触发：监听 v* 开头的 tag（如 v0.1.0, v1.0.0-rc.1）
# 预置 docker build & push 步骤（注释掉，后续填 registry 凭据后启用）
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build release artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'pnpm'
          cache-dependency-file: 'pnpm-lock.yaml'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm -r --filter=./apps/* build

      - name: Extract tag version
        id: version
        run: |
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-build-${{ steps.version.outputs.version }}
          path: |
            apps/*/dist
            apps/*/.next
            apps/*/build
          retention-days: 30
          if-no-files-found: ignore

  # Docker 构建与推送：启用时填入正确的 registry 凭据
  # 需要 Secrets:
  #   DOCKER_REGISTRY       - 镜像仓库地址，如 registry.cn-hangzhou.aliyuncs.com
  #   DOCKER_USERNAME       - 仓库账号
  #   DOCKER_PASSWORD       - 仓库密码/Token
  #   IMAGE_NAME            - 镜像名称，如 hair-salon/server
  # docker:
  #   name: Build & push Docker images
  #   runs-on: ubuntu-latest
  #   needs: build
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       service: [server, admin, mini]
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Set up QEMU
  #       uses: docker/setup-qemu-action@v3
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Login to Docker registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ secrets.DOCKER_REGISTRY }}
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #
  #     - name: Extract metadata
  #       id: meta
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: ${{ secrets.DOCKER_REGISTRY }}/${{ secrets.IMAGE_NAME }}-${{ matrix.service }}
  #         tags: |
  #           type=ref,event=tag
  #           type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
  #           type=sha,prefix=,format=short
  #         labels: |
  #           org.opencontainers.image.title=hair-salon-${{ matrix.service }}
  #           org.opencontainers.image.source=${{ github.repositoryUrl }}
  #
  #     - name: Build and push
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./apps/${{ matrix.service }}
  #         file: ./apps/${{ matrix.service }}/Dockerfile
  #         platforms: linux/amd64,linux/arm64
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}
  #         labels: ${{ steps.meta.outputs.labels }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
